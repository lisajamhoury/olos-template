// values of volume in dB
// minimum is -Infinity, maximum is 0
const hihatGain = -4;
const kickGain = -6;
const padGain = -9;
const melodyGain = -5;

// beats per minute
const globalBPM = 60;

const hihatChance = 0.1;
const kickChance = 1.0;
const melodyChance = 0.5;
const padChance = 1.0;

export {
  globalBPM,
  hihatGain,
  kickGain,
  melodyGain,
  padGain,
  hihatChance,
  kickChance,
  melodyChance,
  padChance,
};
